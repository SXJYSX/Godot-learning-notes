你的直觉非常敏锐！**全局监听 `get_tree().node_added` 确实存在明显的性能隐患。**

### 为什么全局监听 `node_added` 容易成为性能瓶颈？

当你在 `SceneTree` 全局层挂载 `node_added` 信号时，**游戏中创建的任何一个 Node** 都会触发一次回调：

- 敌人发射的每一颗子弹（Bullet）
    
- 各种粒子系统（Particle）、音效播放器（AudioStreamPlayer）
    
- 动态生成的 UI 元素、Timer 节点、TileMap 内部节点等
    

如果场景里正经历弹幕爆发或频繁生成节点（比如一帧内生成 200 个子弹），C++ 底层的 `node_added` 信号就会疯狂回调到 GDScript 层 200 次。每次回调都要执行一次类型判定（`if node is Heart`）和组判定（`node.is_in_group(...)`），这在频繁节点生成时会引入不必要的逻辑开销。

### 更好的 2 种高性能替代方案

在工业级开发中，我们有更精准、开销更小的方案来替代全局监听：

#### 方案 1：局域容器监听 `child_entered_tree`（作用域隔离）

不要监听整棵场景树，而是**只监听存放道具的父节点**（如关卡中的 `$Items` 节点）：

GDScript

```
# level_controller.gd
@onready var items_container: Node2D = $Items # 专用于存放动态生成道具的文件夹节点

func _ready() -> void:
	# 仅当 $Items 内部被添加子节点时才触发回调
	items_container.child_entered_tree.connect(_on_item_entered)

func _on_item_entered(node: Node) -> void:
	if node is Heart:
		_register_heart(node)
```

- **性能对比**：开销直接从 $O(\text{全游戏生成的所有节点数})$ 骤降为 $O(\text{生成的道具数})$，完全避开了子弹、粒子等无关节点的干扰。
    

#### 方案 2：显式事件总线广播 `EventBus`（主动推模式，最推荐）

由**生成者（宝箱、敌人等）在生成道具时，显式通过全局 EventBus 抛出事件**，完全不依赖任何树节点的事件监听。

GDScript

```
# 1. event_bus.gd (Autoload 单例)
signal item_spawned(item: Node)

# 2. enemy.gd / chest.gd (宝箱/敌人掉落)
func drop_heart() -> void:
	var heart := HEART_SCENE.instantiate()
	get_parent().add_child(heart)
	# 生成完成后，显式广播
	EventBus.item_spawned.emit(heart)

# 3. level_controller.gd (关卡控制器)
func _ready() -> void:
	EventBus.item_spawned.connect(_on_item_spawned)

func _on_item_spawned(item: Node) -> void:
	if item is Heart:
		_register_heart(item)
```

- **性能对比**：**$O(1)$ 精准触发**。只有在“真的有道具被创建出来”的那一瞬间才会发一条信号，零额外检索与校验开销，是性能最优且最解耦的方式。
    

### 总结选择指南

- **少量静态关卡**：在 `_ready()` 里用 `get_tree().get_nodes_in_group()` 初始化拉取一次即可。
    
- **局部动态生成（如掉落物）**：采用 **方案 2 (`EventBus.item_spawned`)**，精准高效。