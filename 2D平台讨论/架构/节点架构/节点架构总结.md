玩家也应该有一个PlayerBrain

未来可能会有的节点：
```
Player (CharacterBody2D)                     ← [根节点] 挂载主脚本，掌控全局物理与逻辑
├── Effects (Node2D)                         ← [固定特效/光源] 不跟随方向翻转的视觉元素
│   ├── FeetParticles (GPUParticles2D)       ← 脚下起跳/落地粒子（设置 local_coords = false）
│   └── AmbientLight (PointLight2D)          ← 角色中心自发光/黑夜剪影照明
└── Pivot (Node2D)                           ← [翻转枢纽] 所有需要跟随左右面向的节点
    ├── HandLight (PointLight2D)             ← 随手部/面向变化的偏心光源（如提灯、枪口光）

```

## 玩家第五版

Omni (Node2D)                        ← 【全向容器】保持 scale(1,1)，不随转向翻转
│
│  ├─── [被动靶子区] (monitoring = false, monitorable = true) ─────────────
├── PlayerHurtbox (Area2D)           ← [被动受击] 战斗受击靶（供敌人 Hitbox 主动检测）
├── FootTarget (Area2D)              ← [被动踩踏] 脚底机关靶（供压力板/陷阱 Detector 主动检测）
├── MechanismTarget (Area2D)         ← [被动感应] 全身机关靶（供自动门/机关 Detector 主动检测）
│
│  ├─── [主动检测区] (monitoring = true, monitorable = false) ─────────────
├── ItemDetector (Area2D)            → [主动收集] 挂脚本，主动扫描并拾取 ItemReceiver     ( 备选名 Collectible )
├── EventDetector (Area2D)           → [主动检测] 挂脚本，主动感知并踩入 EventZone / EventTrigger
├── EnvironmentDetector (Area2D)     → [主动检测] 挂脚本，主动读取 EnvironmentZone 物理数据
└── FootDetector (ShapeCast2D)       →[主动探测] 挂脚本，主动探测脚底冰面/传送带物理介质

## 玩家第四版 按照下面来做，把TriggerReceiver拆成三个
### 拆分后的 Scene 场景树架构


```
Omni (Node2D)                        ← 【全向容器】保持 scale(1,1)，不随转向翻转
├── PlayerHurtbox (Area2D)           ← [战斗受击] 仅监听 Hitbox
├── ItemReceiver (Area2D)            ← [道具拾取] 仅监听 ItemEmitter
├── EventReceiver (Area2D)           ← [剧情/切关] 仅监听 EventTrigger
├── MechanismTarget (Area2D)       ← [机关感知] 仅监听 MechanismEmitter
├── EnvironmentDetector (Area2D)     ← [主动检测] 仅监听 EnvironmentZone
├── FootDetector (ShapeCast2D)       ← [脚底物理介质]
└── FootTarget (Area2D)              ← [脚底机关靶子]
```

Detector 对应 Target
Emitter 对应 Receiver
关于环境 xx_zone，直接将 `Zone` 视作 `Target` 家族在“世界环境”分类下的标准化命名：

```
 Detector (主动逻辑方)         Target / Zone (被动数据方)
────────────────────────     ─────────────────────────────
 EnvironmentDetector    <──>  GravityZone / DotZone    (仅含 @export 数据)
 FootDetector           <──>  FloorTarget              (纯物理标志，零脚本)
 MechanismDetector      <──>  MechanismTarget          (纯物理标志，零脚本)
```

- **带 `Target` 后缀**：纯粹的碰撞标志，甚至**不需要挂脚本**。
- **带 `Zone` 后缀**：环境空间标志，**挂载纯数据脚本**（仅 `@export` 配置，无逻辑方法）。

## 玩家第三版
结合我们刚才关于“物理控制权”与“脚底环境”的讨论，重新调整后的 Player 节点架构树如下：

Plaintext

```
Player (CharacterBody2D)                     ← [根节点] 掌控全局物理与逻辑
├── PhysicsCollision (CollisionShape2D)      ← [主物理体] 挡墙踩地（居中，不翻转）
├── Timers (Node)                            ← [定时器容器] 统一收纳 Coyote, Dash 等 Timer
│   └── CoyoteJumpTimer (Timer)
├── Sensors (Node2D)                         ← [传感器总管] 挂载 sensors.gd 脚本
│   ├── Omni (Node2D)                        ← 【全向容器】保持 scale(1,1)，不随转向翻转
│   │   ├── PlayerHurtbox (Area2D)           ← [被动接收] 战斗受击（胸口极小核心，判定弹幕与近战）
│   │   ├── PlayerTriggerReceiver (Area2D)   ← [被动接收] 道具拾取/剧情切入/机关感知（被 TriggerZone 覆盖）
│   │   ├── EnvironmentDetector (Area2D)     ← [主动检测] 水域/毒气/强风（读取环境 Data，直接扣血/改物理）
│   │   ├── FootDetector (ShapeCast2D)       ← [主动检测] 脚底物理介质（主动读取传送带推力、冰面摩擦力、弹簧）
│   │   └── FootTarget (Area2D)              ← [被动接收] 脚底机关靶子（纯被动踩下压力板、踩空落石陷阱）
│   └── Directional (Node2D)                 ← 【定向容器】方向跟随面向，节点本身保持 scale(1,1)
│       ├── InteractableCast (RayCast2D)     ← [主动检测] 正前方 NPC/宝箱主动交互（弹 E 键提示）
│       └── WallCast (RayCast2D)             ← [主动检测] 前方蹬墙/滑墙检测
├── Pivot (Node2D)                           ← [视觉枢纽] 随意 scale.x = -1
│   └── AnimatedSprite2D (AnimatedSprite2D)
├── PlayerBrain (Node)                       ← 读取玩家按键/手柄输入，生成统一 Command
└── StateMachine (Node)                      ← 状态机解算，修改根节点 velocity
    ├── IdleState (Node)
    ├── MoveState (Node)
    └── JumpState (Node)
```

括号外 是 ”预制体“.tscn ， 括号内 是 "挂载的脚本类型".gd
以PlayerHurtbox为例，实际名称是player_hurtbox，为了方便阅读，此处遵照Unity的习惯
箭头 代表了 【逻辑发起方 → 逻辑接收方】

PlayerHurtbox（hurtbox） ← HazardHitbox()
ItemReceiver(item_receiver) ← ItemEmitter  拾取道具（item_emitter）

EnvironmentDetector → WaterZone (environment_zone 配置文件)
					DotZone 持续掉血(environment_zone )
					
配置文件的意思是：仅包含数据，供其它脚本读取

这版有合并：
传感器 新增了 FootTarget
传感器 TriggerReceiver 改为了 PlayerTriggerReceiver，增加了描述

## 玩家第二版

```
Player (CharacterBody2D)                     ← [根节点] 掌控全局物理与逻辑
├── PhysicsCollision (CollisionShape2D)      ← [主物理体] 挡墙踩地（居中，不翻转）
├── Timers (Node)                            ← [定时器容器] 统一收纳 Coyote, Dash 等 Timer
│   └── CoyoteJumpTimer (Timer)
├── Sensors (Node2D)                         ← [传感器总管] 挂载 sensors.gd 脚本
│   ├── Omni (Node2D)                        ← 【全向容器】保持 scale(1,1)，不随转向翻转
│   │   ├── PlayerHurtbox (Area2D)           ← [被动接收] 战斗受击/尖刺陷阱（触发受击动作、无敌帧）
│   │   ├── TriggerReceiver (Area2D)         ← [被动接收] 道具拾取/剧情切入（踩中 TriggerZone 触发）
│   │   ├── EnvironmentDetector (Area2D)     ← [主动检测] 水域/毒气/强风（读取环境 Data，直接扣血/改物理，无视无敌帧）
│   │   └── FootReceiver (Area2D)            ← [被动接收] 脚底专用（被弹簧 Spring 弹飞、踏上传送带）
│   └── Directional (Node2D)                 ← 【定向容器】方向跟随面向，节点本身保持 scale(1,1)
│       ├── InteractableCast (RayCast2D)     ← [主动检测] 正前方 NPC/宝箱主动交互（弹 E 键提示）
│       └── WallCast (RayCast2D)             ← [主动检测] 前方蹬墙/滑墙检测
├── Pivot (Node2D)                           ← [视觉枢纽] 随意 scale.x = -1
│   └── AnimatedSprite2D (AnimatedSprite2D)
├── PlayerBrain (Node)                       ← 读取玩家按键/手柄输入，生成统一 Command
└── StateMachine (Node)                      ← 状态机解算，修改根节点 velocity
    ├── IdleState (Node)
    ├── MoveState (Node)
    └── JumpState (Node)
```

## 玩家第一版
```
Player (CharacterBody2D)                     ← [根节点] 挂载主脚本，掌控全局物理与逻辑
├── PhysicsCollision (CollisionShape2D)      ← [主物理体] 挡墙踩地（居中，不翻转）
├── Timers (Node)                            ← [定时器容器] 统一收纳 Coyote, Dash 等 Timer
│   └── CoyoteJumpTimer (Timer)
└── Sensors (Node2D)                              <-- 挂载 sensors.gd 脚本
    ├── Omni (Node2D)                             <-- 【全向容器】无需代码处理，静置即可
    │    ├── PlayerHurtbox (Area2D)               <-- 【战斗】被动受击、极小判定、无敌帧
    │    └── TriggerDetector (Area2D)           <-- 【非战斗】道具拾取、剧情触碰，较大
    └── Directional (Node2D)                      <-- 【定向容器】翻转时仅遍历此节点
          └── InteractableCast (RayCast2D/ShapeCast2D) <-- 前方 NPC 交互

          
├── Pivot (Node2D)                           <-- 仅放纯视觉节点 (Sprite2D/AnimationPlayer)，可随意 scale.x = -1
│   └── AnimatedSprite2D (AnimatedSprite2D)
├── PlayerBrain  (Node)                      <-- 读取玩家输入
├── StateMachine  (Node)                     <-- 动作类节点，修改根节点(CharacterBody2D)的velocity【通过依赖注入+调用的方法】
│   ├── IdleState (Node)
│   ├── MoveState (Node)
│   └── JumpState (Node)
```

## 敌人
```
Goblin (CharacterBody2D)                     ← [根节点] 挂载主脚本，掌控全局物理与逻辑
├── PhysicsCollision (CollisionShape2D)      ← [主物理体] 挡墙踩地（居中，不翻转）
├── Timers (Node)                            ← [定时器容器] 统一收纳 Coyote, Dash 等 Timer
│   └── XXTimer (Timer)
└── Sensors (Node2D)                              <-- 挂载 sensors.gd 脚本
    ├── Omni (Node2D)                             <-- 【全向容器】无需代码处理，静置即可
    │    └── EnemyHurtbox (Area2D)                <-- 【战斗】被动受击，无敌帧
    └── Directional (Node2D)                      <-- 【定向容器】翻转时仅遍历此节点
          ├── WallDetector (RayCast2D)             <-- 前方撞墙检测
          └── CliffDetector (RayCast2D)            <-- 前方悬崖检测      
├── Pivot (Node2D)                           <-- 仅放纯视觉节点 (Sprite2D/AnimationPlayer)，可随意 scale.x = -1
│   └── AnimatedSprite2D (AnimatedSprite2D)
├── GoblinBrain   (Node)                      <-- 哥布林的行为模式，【调用传感器、切换条件】 都写在这里
│   ├── IdleMode  (Node)                      <-- 具体的行为模式，不同敌人可复用
│   ├── PatrolMode(Node)
│   └── ChaseMode (Node)
├── StateMachine  (Node)                     <-- 动作类节点，修改根节点(CharacterBody2D)的velocity【通过依赖注入+调用的方法】
│   ├── IdleState (Node)
│   ├── MoveState (Node)
│   └── JumpState (Node)
```
参考资料：
[[节点架构总结]]
[[玩家节点树拓扑结构（非状态机）]]
[[我理想中的敌人状态机]]

补充：
Area2D类型的节点 必定有一个CollisionShape2D的子节点
# 节点解析

### Player
读取用户输入，把输入传给
物理帧执行顺序：
### StateMachine
有自定义脚本，敌人和玩家的StateMachine都是一样的。
存储玩家or敌人的 所有可能的动作。
### Sensors
sensors.gd   只提供工具方法，比如 令所有需要更新的子节点更新检测。这些子节点都是 ShapeCast2D 或者 RayCast2D 类型。
sensors.gd   不会精细地控制某个传感器的开与关——那是XXBrain.gd要做的事
sensors.gd   没有自更新，末端传感器 没有脚本。

能自行后拖过手动找到子节点和孙子节点的引用，配合执行下面两个方法。

有一些节点会主动发起逻辑
有方法：令子节点们的传感器都更新。
有方法：配合 【功能：翻转】，修改Directional的scale.x

### Sensor执行顺序
[1. 修改朝向与射线] ──> facing *= -1; ray.target_position.x *= -1
                             │
[2. 强制刷新射线]   ──> ray.force_raycast_update()  <-- 此时 AI 拿到绝对准确的最新只读数据！
                             │
[3. 更新移动速度]   ──> velocity.x = facing * speed <-- 决策层根据最新数据把速度设为反向
                             │
[4. 物理提交]       ──> move_and_slide()            <-- 引擎带着反向速度把角色拉离墙体
                             │
[5. 刷新内置状态]   ──> is_on_wall() 自动更新为 false
---
以哥布林为例（一种敌人）
### Goblin
读取用户输入，把输入传给
物理帧执行顺序：

### GoblinBrain
哥布林的大脑就是哥布林的大脑，这是哥布林的行为模式。
根据检测结果，切换状态的判定条件写在这里。
它的每个子节点对应一个行为模式，行为模式可以在不同敌人之间复用。
#### Brain
作为基类，有自定义变量，用于存储【移动摇杆、跳跃键、攻击键、其它指令（比如字符串）】
这些都是
### IdleMode
不是敌人的精确移动，而是模拟【移动摇杆、跳跃键、攻击键、其它指令（比如字符串）】
修改Brain的自定义变量

### StateMachine
直接读取 XXBrain

---
# 功能实现
### 翻转
需要令 Pivot 和 Directional (Sensors的子节点) 的scale.x 的值 为 1或 -1
[[哪些传感器可以缩放scale.x]]

---
### Goblin 物理帧内执行顺序（从上至下）

Plaintext

```
_physics_process(delta) 启动
│
├── 1. 传感器感知 (Sensors Update)    <-- 采样/打包当帧环境数据
├── 2. Brain 决策 (Brain Strategy)     <-- 根据数据切换 BehaviorMode
├── 3. 模拟输入 (Mode Command)          <-- 当前 Mode 输出模拟手柄向量
├── 4. 状态机解算 (State Physics)       <-- 当前 State 根据模拟输入计算 velocity
├── 5. 物理提交 (move_and_slide)        <-- 引擎推演碰撞位移，产生最新物理事实
├── 6. 物理后确认与视觉更新 (Post & Visual) <-- 刷新事实标志位，刷新 Pivot 动画
```

### 玩家与敌人的“终极物理流水线”对齐表
表内的2 对应上面的 2+3

|**阶段**|**逻辑职责**|**玩家 (Player) 怎么做**|**敌人 (Goblin) 怎么做**|
|---|---|---|---|
|**1. 输入与感知** _(Perception)_|收集决策所需的数据|**人类按键** (`Input.get_axis`) + 人眼看屏幕|**`Sensors.update_all()`**（传感器搜寻墙壁/玩家）|
|**2. 决策与计算** _(Decision & Math)_|根据数据计算速度向量 `velocity`|你的代码：<br><br>  <br><br>· `apply_gravity()`<br><br>  <br><br>· `handle_jump()`<br><br>  <br><br>· `apply_acceleration()`|**`GoblinBrain.think()`**<br><br>  <br><br>根据传感器数据让 `PatrolMode` 或 `IdleMode` 生成速度向量|
|**3. 物理提交** _(Execution)_|让物理引擎解算移动与碰撞|**`move_and_slide()`**|**`move_and_slide()`**|
|**4. 物理后检测** _(Post-Process)_|读取**移动结算后**的真实状态|检查 `was_on_floor` 与 `is_on_floor()`，刷新**土狼时间**与**二段跳**次数|检查是否撞墙 / 重新重置某些单帧标志位|
|**5. 表现层更新** _(Visual)_|渲染视图|**`update_animations()`**|**`update_animations()`**|