# 命名规范 与 代码结构设计笔记
> 基于 Godot 4，从Unity转Godot的开发者视角

---

## 一、GDScript 命名规范

| 类型 | 规范 | 示例 |
|---|---|---|
| 类名 | `PascalCase` | `PlayerMovementData` |
| 方法名 | `snake_case` | `handle_jump()` |
| 公开变量 | `snake_case` | `jump_velocity` |
| 私有变量 | `_snake_case` | `_jump_velocity` |
| 常量 | `SCREAMING_SNAKE_CASE` | `MAX_SPEED` |
| 信号 | `snake_case` | `health_changed` |
| 枚举类型名 | `PascalCase` | `StateType` |
| 枚举值 | `SCREAMING_SNAKE_CASE` | `StateType.DEAD` |

### 与 C# 的关键差异
方法名是最大的差异：C# 用 `PascalCase`，GDScript 用 `snake_case`。

### 文件名规范
- `.gd` 文件名用 `snake_case`：`player_movement_data.gd`
- `class_name` 用 `PascalCase`：`PlayerMovementData`
- 两者是同一个名字的不同格式，内容完全对应（和C#的 `PlayerMovementData.cs` 逻辑相同）

### GDScript 大小写敏感
`speed`、`Speed`、`SPEED` 是三个完全不同的变量。

---

## 二、访问权限

GDScript **没有** `private`、`protected`、`public` 关键字，只有命名约定：

```gdscript
var speed = 100.0       # 约定为公开
var _speed = 100.0      # 约定为私有（仅约定，语言不强制）

func jump():            # 约定为公开
    pass

func _apply_gravity():  # 约定为私有（仅约定，语言不强制）
    pass
```

⚠️ 注意：Godot 内置生命周期方法（`_ready`、`_physics_process`）也是下划线开头，但这不是因为"私有"，只是引擎的命名惯例。

### 如何发现误用私有方法？
- 加类型注解，编辑器能提供更多静态分析
- `_ready()` 里用 `assert` 做前置检查
- 开启项目设置中的 `Enable Type Warnings`
- 搜索方法名时加括号：搜 `handle_jump(` 而不是 `handle_jump`，过滤注释噪音

---

## 三、GDScript 属性语法（快捷方式）

类似 C# 的 `private float Speed => movement_data.speed`，GDScript 写法：

```gdscript
var speed: float:
	get: return movement_data.speed

var acceleration: float:
	get: return movement_data.acceleration

var friction: float:
	get: return movement_data.friction

var jump_velocity: float:
	get: return movement_data.jump_velocity
```

这样在代码里直接写 `speed` 等价于 `movement_data.speed`，提高可读性。

⚠️ 缩进必须用 **Tab**，不能用空格，否则会产生大量连锁报错。

---

## 四、值类型 vs 引用类型

| 值类型（复制） | 引用类型（共享） |
|---|---|
| `int`、`float`、`bool` | `Object` 及其子类 |
| `Vector2`、`Vector3` | `Node`、`Resource` |
| `Color`、`Rect2` | `Array`、`Dictionary` |

`Resource` 是引用类型，赋值给另一个变量不会复制数据：

```gdscript
var _data = movement_data  # _data 和 movement_data 指向同一个对象
```

`Array` 和 `Dictionary` 是引用类型，容易踩坑。

---

## 五、文件夹管理

### 推荐结构
```
res://
├── scripts/
│   └── resources/          # Resource 模板脚本（定义数据结构）
│       └── player_movement_data.gd
│
└── resources/              # 实例数据（真正的资产文件）
    └── player/
        ├── normal_movement.tres
        ├── ice_movement.tres
        └── underwater_movement.tres
```

- `scripts/resources/` → **代码**，定义数据结构的模板脚本
- `resources/` → **数据**，真正的资产文件（.tres）

### 特殊文件夹
Godot 中需要注意的自动生成文件夹（不要手动修改）：
- `.import/`：导入资源的缓存数据
- `.godot/`：编辑器缓存、脚本类型信息

Godot **没有**类似 Unity `Resources/` 那样具有特殊运行时意义的文件夹。

---

## 六、NPC 与玩家复用移动逻辑

### 核心思路
角色的移动逻辑只关心"输入数据"，不关心"输入来源"。

```gdscript
# 玩家输入
class_name PlayerInputHandler
static func get_input() -> Dictionary:
    return {
        "axis": Input.get_axis("move_left", "move_right"),
        "jump": Input.is_action_just_pressed("jump"),
    }

# NPC输入（AI决策）
class_name NpcInputHandler
static func get_input(npc_state) -> Dictionary:
    return {
        "axis": _calculate_move_direction(npc_state),
        "jump": _should_jump(npc_state),
    }
```

玩家和 NPC 共用同一套移动逻辑（`handle_acceleration`、`apply_friction` 等），只需换不同的 `InputHandler`。

---

## 七、敌人代码结构设计

### 核心原则：组合优于继承

❌ **不推荐：继承树**（兔子既会跳又会巡逻时，该放哪里？）
```
BaseEnemy → WalkingEnemy → PatrolEnemy
          → JumpingEnemy → RabbitEnemy
```

✅ **推荐：组合**（按需挂载行为组件）
```
Rabbit（场景）
├── CharacterBody2D
├── JumpMovement        # 跳跃移动组件
├── PatrolBehavior      # AB巡逻组件
└── FacePlayer          # 面向玩家组件

Guard（场景）
├── CharacterBody2D
├── WalkMovement        # 走路移动组件
├── PatrolBehavior      # 复用同一个组件
└── FacePlayer          # 复用同一个组件
```

### 哪些逻辑写在代码中，哪些体现在子节点中？

> **骨架写在脚本里，器官挂在子节点上。**

| 写在脚本（骨架） | 挂在子节点（器官） |
|---|---|
| 核心物理：重力、速度、`move_and_slide()` | 可复用行为：巡逻、追击、面向玩家 |
| 状态机：决策逻辑 | 感知范围：`Area2D` 检测玩家 |
| 输入处理：意图转数据 | 攻击判定：`Area2D` 检测命中 |

骨架是**每个角色都必须有的**，器官是**可选的、可组合的**。

### 结合状态机的完整分层

| 层级 | 负责 |
|---|---|
| 状态机 | 决策：现在该做什么 |
| 行为组件 | 生成输入意图数据 |
| 移动逻辑 | 执行：把意图变成 velocity |
